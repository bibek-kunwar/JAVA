package FileHandlingJava;
/*
import java.util.Scanner;

public class CountVowelConsonant {

    public static void main(String[] args) {
        String s=new String();

        Scanner read = new Scanner(System.in); // Input stream is used

        System.out.print("Enter any name: ");
        String name = read.nextLine();
        int i,vowels,Cons;
        vowels=Cons=0;

        for(i=0;i<s.length();i++){
            s=s.toLowerCase();    //Convert the string to lowercase
            for( i=0;i<s.length();i++)
            {
                if(s.charAt(i)=='a' || s.charAt(i)=='e' || s.charAt(i)=='i' || s.charAt(i)=='o' ||
                        s.charAt(i)=='u')
                    vowels++;  //Increment each time vowel encounters
                else if(s.charAt(i) >= 'a' && s.charAt(i)<='z')
                    Cons++;   //Increment each time consonant encounters
            }
            //Print the total number of vowels
            System.out.println("The total number of vowels is: "+vowels);
            //Print the total number of consonants
            System.out.println("The total number of consonants is: "+Cons);
        }
        }

    }
}

 */